version: "3"

services:
  icevision:
    build:
      dockerfile: Dockerfile
      context: .
    image: ice
    ports:
      - "8889:8889"
    tty: true
    volumes:
      - /path/to/notebook/files/:/workspace/
      - /path/to/annotation/data/:/data/
